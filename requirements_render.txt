# ============================================================
# Render Deployment Requirements (CPU-only)
# ============================================================
# This file is used by Dockerfile and Render build for the
# FastAPI live-detection WebSocket server (live_server.py).
# Model weights are downloaded at container start via startup_config.py.
# ============================================================

# Core ML / audio
torch==2.1.2
torchaudio==2.1.2
numpy==1.24.4
scipy==1.11.4
librosa==0.10.1
soundfile==0.12.1

# FastAPI & async server
fastapi==0.110.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# HuggingFace / transformers (for Wav2Vec2 backbone)
transformers==4.40.2
huggingface-hub==0.23.0
tokenizers==0.19.1
safetensors==0.4.3

# s3prl (self-supervised speech representations)
s3prl==0.4.15

# Utilities
requests==2.31.0
tqdm==4.66.1
gdown==5.2.0
certifi
PyYAML==6.0.1
packaging==24.0
filelock==3.13.1
regex==2024.4.16
